# QQ机器人工程化项目 — 里程碑实施 Checklist

**技术栈**：LangChain (1.0) + OneBot v11 (NapCat) + Python
**使用方式**：跟着 Claude Code 一起实现，但标注 🧠 的部分必须你自己先动手设计/编写

---

## 图例

- 🧠 **你必须自己做** — 设计决策、核心逻辑，不能外包给 AI
- 🤖 **Claude Code 可以帮你** — 样板代码、排查、补全
- 📐 **工程实践点** — 这一步在练什么能力

## 协作总则（贯穿全程，先读一遍）

- [ ] 每次用 Claude Code 前，先自己写伪代码/设计草图，带着"我想要什么结构"去对话
- [ ] AI 生成代码后，逐行读一遍，能讲出每一行作用再接受
- [ ] 优先让 AI 做 review（"这里有没有更好写法"），少让它直接代写核心逻辑
- [ ] 每个里程碑完成后，在 `docs/journal/` 写一段复盘笔记：我设计了什么、踩了什么坑、AI帮了什么忙

---

## 里程碑 0：项目初始化

### 🧠 你必须自己做

- [ ] 写第一篇 ADR：`docs/decisions/ADR-001-dependency-tool.md`，记录依赖管理工具选型的理由（uv / poetry / pip 的权衡）
- [ ] 手动搭建目录骨架：
  ```bash
  mkdir -p qq-langchain-bot/{onebot_adapter,core,agent/tools,agent/middleware,memory,config,tests,scripts,data,docs/decisions,docs/journal}
  cd qq-langchain-bot && git init
  ```
- [ ] 手写 `.gitignore`（至少覆盖：`data/`、`.env`、`__pycache__/`、`*.db`、`.venv/`），自己想清楚每一行为什么在那
- [ ] 手写 `docs/CODING_STANDARDS.md`，至少包含：
  - [ ] 类型标注 + docstring 规范
  - [ ] 异常处理规范（禁止裸 `except:`）
  - [ ] 日志 vs print 的规范
  - [ ] 模块公共接口导出规范

### 🤖 Claude Code 可以帮你

- [ ] 生成 `pyproject.toml` 骨架（你提供项目名/Python版本/依赖管理工具）
- [ ] 生成 pre-commit 配置（ruff + mypy + pytest 钩子）
- [ ] 检查 `.gitignore` 是否有遗漏

### ✅ 验收标准

- [ ] `git log` 有至少一次规范 commit（如 `feat:` / `chore:` 前缀）
- [ ] `docs/decisions/ADR-001-*.md` 是你自己写的
- [ ] `pre-commit run --all-files` 能跑通
- [ ] 能脱稿讲清楚为什么选这个依赖管理工具

### 📐 工程实践点
项目动工前先有清晰边界和规范，是"能跑的脚本"和"能维护的项目"的分水岭。

---

## 里程碑 1：OneBot 协议数据模型

### 🧠 你必须自己做

- [ ] 通读 [OneBot v11 官方文档](https://github.com/botuniverse/onebot-11) 的事件部分
- [ ] 纸上画一张事件类型层级图（`post_type` → `sub_type` 的分类关系）
- [ ] 手写 `onebot_adapter/event.py` 第一版模型骨架（哪怕写得丑）
- [ ] 自己设计"条件必填"逻辑：`message_type="group"` 时 `group_id` 必填、`private` 时为 `None`，用 Pydantic `model_validator` 实现
- [ ] 自己决定：`message` 字段用数组格式还是兼容 CQ 码字符串格式（并写下理由）

### 🤖 Claude Code 可以帮你

- [ ] Review 你写的模型，检查有没有漏掉协议文档里的字段
- [ ] 补全单元测试骨架（你再检查是否真的覆盖了关心的场景）
- [ ] 生成从示例 JSON 转 test fixture 的脚本

### ✅ 验收标准

- [ ] 能用你的 Pydantic 模型成功解析：群消息、私聊消息、心跳事件三种示例 JSON
- [ ] 故意构造缺字段的错误 JSON，模型正确抛出验证错误（不是静默出错）
- [ ] 单元测试覆盖：群消息解析 / 私聊消息解析 / 未知 post_type 容错
- [ ] 能脱稿讲清楚 `message_type` 和 `sub_type` 的区别

### 📐 工程实践点
面对外部协议时的防御性数据层设计——外部输入永远不可信。

---

## 里程碑 2：OneBot WebSocket 客户端

### 🧠 你必须自己做

- [ ] 画连接状态转换图：`断开 → 连接中 → 已连接 → 异常断开 → 重连中 → ...`，想清楚每个状态下能做什么
- [ ] 自己实现指数退避重连算法，理解并写出：
  - [ ] 为什么要有最大延迟上限
  - [ ] 为什么要加随机抖动（jitter）
- [ ] 自己设计 `call_action` 的 echo 匹配 + 超时逻辑
- [ ] 自己想清楚：WS 断开时，正在等待的 Future 该如何处理（不能永远 pending）

### 🤖 Claude Code 可以帮你

- [ ] Review 你的状态机，帮你找漏掉的边界情况（用提问方式，而非直接代写）
- [ ] 写心跳超时检测的样板代码
- [ ] 写压力测试脚本（模拟频繁断线，验证无资源泄漏）

### ✅ 验收标准

- [ ] 真实连上 NapCat，能收到心跳事件（`meta_event` / `heartbeat`）
- [ ] 手动重启 NapCat 容器，客户端能无人工干预自动恢复连接
- [ ] 断连期间调用 `call_action`，能得到明确超时反馈而非卡死
- [ ] `asyncio` 调试模式跑几分钟，无未 await 协程警告

### 📐 工程实践点
长连接服务的鲁棒性设计——很多"能跑demo但生产会挂"的项目都栽在这一步。

---

## 里程碑 3：打通最小闭环（Echo 机器人）

### 🧠 你必须自己做

- [ ] 自己设计 `core/dispatcher.py` 的过滤规则：哪些消息不该被处理（机器人自己发的、非法类型等）
- [ ] 自己实现消息去重（幂等性）：选数据结构（如 LRU），想清楚为什么不用无限增长的 set
- [ ] 自己写 `MessageSender` 分段发送逻辑，自己决定切割策略（按字节 vs 字符），处理中英文混排不切断词的问题

### 🤖 Claude Code 可以帮你

- [ ] 写集成测试：起假 WS server 模拟 NapCat，验证 echo 全链路
- [ ] 检查分段逻辑在极端输入（纯 emoji、无空格长字符串）下是否出错

### ✅ 验收标准

- [ ] 真实 QQ 群 @机器人，机器人原样回复
- [ ] 连续快速发 5 条消息，不重复、不遗漏回复
- [ ] 发超过 600 字长消息，分段处理正确无崩溃
- [ ] `git log` 显示这一步是多个小提交，不是一个巨大提交

### 📐 工程实践点
分层架构的"通"——很多项目单层测试都过，连起来才发现接口契约不一致。

---

## 里程碑 4：配置系统与日志规范

### 🧠 你必须自己做

- [ ] 自己设计 `Settings` 类：哪些字段有默认值、哪些必须强制提供（如 API key 不该有默认值）
- [ ] 在 `docs/CODING_STANDARDS.md` 补充日志规范：DEBUG/INFO/WARNING/ERROR 各自的使用场景
- [ ] 自己权衡并写下：日志里要不要打印用户消息原文（隐私考量）
- [ ] 手写启动时的配置校验函数（程序启动即检查配置完整性）

### 🤖 Claude Code 可以帮你

- [ ] 生成 `logging_config.py` 样板（JSON格式化、按天滚动）
- [ ] 检查代码里是否还有残留硬编码值

### ✅ 验收标准

- [ ] 删掉 `.env` 必填项，程序启动立刻报错并提示缺什么
- [ ] 日志文件是结构化输出（可被日志工具解析）
- [ ] 自己 grep 一遍代码，确认无硬编码密钥/地址

### 📐 工程实践点
快速失败（fail fast）原则和可观测性基础。

---

## 里程碑 5：LangChain Agent 最小接入

### 🧠 你必须自己做

- [ ] 通读 LangChain 1.0 `create_agent` 官方文档，理解内部消息状态组织方式
- [ ] 自己撰写 system prompt 初版内容（机器人性格设定是产品决策，不外包给AI）
- [ ] 自己设计 LLM 调用失败（超时/限流/内容拦截）时的用户侧反馈策略

### 🤖 Claude Code 可以帮你

- [ ] 排查 LangChain API 用法问题（版本迭代快，文档可能滞后）
- [ ] 生成 Agent 调用的重试装饰器样板

### ✅ 验收标准

- [ ] QQ 发消息，机器人用真实 LLM 生成回复（非 echo）
- [ ] 断网/超时场景下机器人不卡死，能给合理反馈
- [ ] 能脱稿对比说出为什么用 `create_agent` 而非手写 prompt 拼接

### 📐 工程实践点
使用第三方框架时的边界意识——出问题时要能判断是自己代码还是框架的问题。

---

## 里程碑 6：短期记忆（Checkpointer 打通多轮对话）

### 🧠 你必须自己做

- [ ] 自己设计并实现 `make_thread_id` 函数，想清楚记忆隔离粒度规则（群聊按 群+用户，私聊按 用户）
- [ ] 自己构造测试场景验证隔离性：同一用户在群A/群B是否互不干扰、私聊和群聊呢

### 🤖 Claude Code 可以帮你

- [ ] 排查 SQLite checkpointer 具体 API 用法

### ✅ 验收标准

- [ ] 连续 3 轮有上下文关联的提问（如"我叫小明"→"我叫什么"），机器人正确记住
- [ ] 重启机器人进程后，之前对话上下文能被找回（验证真持久化）
- [ ] 群A、群B 同一人的对话互不影响（用自己写的测试验证）

### 📐 工程实践点
状态管理和数据隔离设计——多租户/多会话系统的核心难点。

---

## 里程碑 7：中期记忆（用户画像）

### 🧠 你必须自己做（这一步几乎全部自己做）

- [ ] 自己从零设计数据库 schema（不要直接抄参考文档），想清楚：
  - [ ] 需要哪些字段
  - [ ] 哪些字段该建索引
  - [ ] `intimacy_score` 的类型和取值范围
- [ ] 写完后再对照任何参考方案，分析差异和原因
- [ ] 自己写 SQL 建表脚本 + 迁移逻辑（哪怕只有一版，也养成"schema变更要有迁移脚本"的习惯）
- [ ] 自己实现存取接口，处理并发写入问题：加锁还是用事务？自己决定

### 🤖 Claude Code 可以帮你

- [ ] Review 你的 schema 设计，指出潜在问题（缺索引、字段类型不合理等）
- [ ] 写数据库操作单元测试骨架

### ✅ 验收标准

- [ ] `docs/schema.md` 存在，自己写清楚每个字段含义和取值范围
- [ ] 并发写入测试：同一用户快速连续触发多次更新，数据不丢失不损坏
- [ ] 能讲清楚为什么某字段要建索引、某字段不需要

### 📐 工程实践点
数据建模能力——设计经得起变化的数据库 schema，是后端工程最考验经验的技能之一，必须自己动手。

---

## 里程碑 8：记忆自动提取（异步任务）

### 🧠 你必须自己做

- [ ] 自己设计"先回复、后台异步提取记忆"的任务调度方式，权衡：
  - [ ] `asyncio.create_task` 是否够用
  - [ ] 进程被杀死时任务跑到一半的数据丢失风险，你能否接受，为什么
- [ ] 自己设计异常隔离：后台任务失败绝不能影响主流程，但也不能悄无声息失败——设计失败如何被记录和监控

### 🤖 Claude Code 可以帮你

- [ ] 帮你写/迭代 LLM 结构化信息抽取的 prompt

### ✅ 验收标准

- [ ] 故意让后台记忆提取任务抛异常，确认主对话流程完全不受影响
- [ ] 日志能追踪到每次记忆提取的成功/失败记录
- [ ] 能解释清楚当前方案在"进程崩溃导致记忆丢失"这个风险上的取舍

### 📐 工程实践点
异步任务的可靠性边界——分布式/异步系统里最容易被忽视的风险点。

---

## 里程碑 9：工具系统与中间件

### 🧠 你必须自己做

- [ ] 自己设计限流中间件策略：按用户还是按会话限流？固定窗口还是滑动窗口？权衡实现复杂度和准确性
- [ ] 自己完整实现至少一个工具（含错误处理、返回给 LLM 时的格式设计）

### 🤖 Claude Code 可以帮你

- [ ] 接入第三方 API 的样板代码（查文档、处理认证）

### ✅ 验收标准

- [ ] 短时间狂发消息触发限流，机器人给出合理提示而非直接失效
- [ ] 工具调用失败（如API超时）时，LLM 能拿到清晰错误信息并合理回应，Agent 循环不崩溃

### 📐 工程实践点
系统防护设计——限流、降级、熔断的小规模实践。

---

## 里程碑 10：测试体系与 CI

### 🧠 你必须自己做

- [ ] 自己审视整个项目，判断哪些地方测试覆盖不够（不要让 AI 决定"够不够"，自己想清楚核心风险点）
- [ ] 自己写 CI（如 GitHub Actions）配置的核心逻辑，理解每一步为什么存在

### 🤖 Claude Code 可以帮你

- [ ] 生成测试骨架、补充边界用例建议
- [ ] 生成 CI yaml 样板

### ✅ 验收标准

- [ ] `pytest --cov` 核心模块（adapter/core/memory）覆盖率 > 70%
- [ ] CI 在 push 后自动跑测试，失败会拒绝合并
- [ ] 能指出项目里哪些地方"故意"没写测试，以及为什么

### 📐 工程实践点
测试策略思维——不是所有代码都值得同等测试投入，学会分辨。

---

## 里程碑 11：容器化部署与可观测性收尾

### 🧠 你必须自己做

- [ ] 自己写 `Dockerfile`（多阶段构建，理解为什么分构建阶段和运行阶段）
- [ ] 自己完整走一遍部署流程，记录每个坑，写成 `docs/DEPLOYMENT.md`（自己写，不是AI生成）

### 🤖 Claude Code 可以帮你

- [ ] 排查 Docker 构建报错
- [ ] 优化镜像体积的建议

### ✅ 验收标准

- [ ] 在一台干净服务器上，跟着你自己写的部署文档能从零跑起来（这是检验文档质量的唯一标准）
- [ ] 容器重启后数据不丢失（验证 volume 挂载正确）

### 📐 工程实践点
交付质量——代码写完只是一半，能让别人（包括未来的自己）顺利部署才是完整交付。

---

## 总览进度表

| 里程碑 | 内容 | 状态 |
|---|---|---|
| 0 | 项目初始化 | ☐ |
| 1 | OneBot 协议数据模型 | ☐ |
| 2 | OneBot WebSocket 客户端 | ☐ |
| 3 | 最小闭环 Echo 机器人 | ☐ |
| 4 | 配置系统与日志规范 | ☐ |
| 5 | LangChain Agent 最小接入 | ☐ |
| 6 | 短期记忆 Checkpointer | ☐ |
| 7 | 中期记忆用户画像 | ☐ |
| 8 | 记忆自动提取（异步任务） | ☐ |
| 9 | 工具系统与中间件 | ☐ |
| 10 | 测试体系与 CI | ☐ |
| 11 | 容器化部署与可观测性收尾 | ☐ |
